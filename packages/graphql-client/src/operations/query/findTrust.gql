query FindTrust($uuid: UUID!) {
  findTrust(uuid: $uuid) {
    uuid
    name
    locales {
      edges {
        node {
          locale
          name
          description
        }
      }
    }
  }
}
