query FindCredential($uuid: UUID!) {
  findCredential(uuid: $uuid) {
    uuid
    name
    locales {
      edges {
        node {
          locale
          name
          description
        }
      }
    }
  }
}
