query FindHandler($uuid: UUID!) {
  findHandler(uuid: $uuid) {
    uuid
    name
    locales {
      edges {
        node {
          locale
          name
          description
        }
      }
    }
  }
}
